
import { courseProgressService } from './course-progress/course-progress-service';

// Re-export to maintain backward compatibility
export { courseProgressService };

// Re-export types
export * from '@/types/course-progress';
